$(document).ready(function(){
    $('.sidenav').sidenav();
    $('.carousel').carousel();
    $('.scrollspy').scrollSpy();
});

const
window.addEventListener('scroll', () => {
    if (window.scrollY > 790) {
        
    }
});