$(document).ready(function(){
    $('.sidenav').sidenav();
    $('.carousel').carousel();
    $('.scrollspy').scrollSpy();
});

window.addEventListener('scroll', () => {
    if (window.scrollY > 1000) {
        
    }
});